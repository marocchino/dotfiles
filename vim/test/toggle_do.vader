Given elixir (single line method):
  defmodule SomeModule do
    def upcase(name) do
      String.upcase(name)
    end
  end
Do (toggle do):
  jtd
Expect elixir (one liner with do:):
  defmodule SomeModule do
    def upcase(name), do: String.upcase(name)
  end
Do (toggle do):
  td
Expect elixir (revert do ... end):
  defmodule SomeModule do
    def upcase(name) do
      String.upcase(name)
    end
  end
