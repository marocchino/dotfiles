#!/bin/bash
if [[ $# -eq 0 ]]; then
  echo "git branch | fzf --height 40% --border | xargs git restore"
  git branch | fzf --height 40% --border | xargs git restore
else
  echo "git restore $*"
  # shellcheck disable=SC2068
  git restore $@
fi
